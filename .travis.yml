osx_image: xcode12.5
language: objective-c
# cache: cocoapods
# podfile: Example/Podfile
# before_install:
# - gem install cocoapods # Since Travis is not always on latest version
# - pod install --project-directory=Example
script:
- set -o pipefail && xcodebuild test -enableCodeCoverage YES -project AbstractTableView.xcodeproj -scheme AbstractTableView -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 11,OS=14.5' ONLY_ACTIVE_ARCH=NO | xcpretty
- pod lib lint
